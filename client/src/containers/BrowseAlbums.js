import React from 'react';

const BrowseAlbums = () => {
  return (
    <div>
      <div>if there are no albums in database, show => no albums</div>
      <div>else</div>
      <div>fetch all albums from database and render it here</div>
    </div>
  );
};

export default BrowseAlbums;
